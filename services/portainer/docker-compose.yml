services:
  portainer:
    image: portainer/portainer-ce:latest
    container_name: nuttz-portainer
    restart: unless-stopped

    ports:
      - "9000:9000"
      - "9443:9443"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ~/AI-Server/data/portainer:/data
