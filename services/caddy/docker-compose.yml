services:
  caddy:
    image: caddy:2.10
    container_name: nuttz-caddy
    restart: unless-stopped

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ../dashboard/site:/srv:ro
      - ./data:/data
      - ./config:/config
